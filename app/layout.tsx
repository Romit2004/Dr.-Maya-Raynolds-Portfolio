import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Dr. Maya Raynolds",
  description: "Dr. Maya Raynolds is a renowned psychologist specializing in cognitive behavioral therapy. With over 20 years of experience, she has helped countless individuals overcome mental health challenges and improve their well-being. Dr. Raynolds is known for her compassionate approach and evidence-based techniques, making her a trusted figure in the field of psychology.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
